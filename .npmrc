# pnpm 10 blocks dependency lifecycle scripts by default. Only esbuild (Vite's
# bundler, which fetches its platform binary) is allowed, via
# `pnpm.onlyBuiltDependencies` in package.json. earshot ships plain TypeScript
# source and needs no scripts of its own.
strict-peer-dependencies=false
