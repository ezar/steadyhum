{
  "$comment": "Model binaries are never committed. `pnpm models:fetch` downloads each entry into public/models/ and verifies its SHA-256. Fill in `sha256` the first time a file is fetched (the script prints the digest it saw) so later runs are reproducible.",
  "files": [
    {
      "name": "yamnet-classifier.task",
      "url": "https://storage.googleapis.com/mediapipe-models/audio_classifier/yamnet/float32/1/yamnet.task",
      "sha256": "",
      "approximateBytes": 4000000,
      "purpose": "Noise guard and machine-likeness sanity check (521 AudioSet classes)."
    },
    {
      "name": "yamnet-embedder.task",
      "url": "https://storage.googleapis.com/mediapipe-models/audio_embedder/yamnet/float32/1/yamnet.task",
      "sha256": "",
      "approximateBytes": 4000000,
      "purpose": "1024-dimensional embedding per 0.975 s window; the primary representation."
    }
  ]
}
